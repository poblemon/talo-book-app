import { default as default_2 } from 'react';

export declare const customElementToReactComponent: <I extends HTMLElement, E extends Record<string, string> = {}>({ react: React, tag, elClass, schemaEvents, }: Options<I, E>) => default_2.ForwardRefExoticComponent<default_2.PropsWithoutRef<E> & default_2.RefAttributes<I>>;

declare type Options<I extends HTMLElement, E extends Record<string, string> = {}> = {
    react: typeof default_2;
    tag: string;
    elClass: {
        new (): I;
    };
    schemaEvents?: E;
};

export { }
